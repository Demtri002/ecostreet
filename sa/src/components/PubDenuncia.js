import React from 'react'
import { View, SafeAreaView, ScrollView} from 'react-native'

const PubDenuncia = () =>{


    return(
        <SafeAreaView>
            <ScrollView>
                <View >
                    
                </View>
            </ScrollView>
        </SafeAreaView>
    )
}

export default PubDenuncia